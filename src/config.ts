const config = {
  dataSourceMetadata: {
    fields: {
      /**
       * Default list of values available for each data field
       */
      maxOptionsCount: 100
    },
    inlineDataSource: {
      maxSizeInBytes: 3000000
    }
  }
};

export default config;
