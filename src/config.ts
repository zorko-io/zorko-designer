const config = {
  dataSourceMetadata: {
    fields: {
      /**
       * Default list of values available for each data field
       */
      maxOptionsCount: 100
    }
  }
};

export default config;
